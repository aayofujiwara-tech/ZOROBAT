# サイコロぞろ目バトル - 通信対戦機能 実装プロンプト

## 概要

既存のプロトタイプ（プレイヤー vs CPU）に、Firebase を使ったリアルタイム通信対戦（プレイヤー vs プレイヤー）を追加してください。

既存のCPU対戦モードはそのまま残し、タイトル画面で「CPU対戦」と「オンライン対戦」を選べるようにします。

---

## 技術選定

- **Firebase Realtime Database**（Firestoreでも可、レイテンシ重視ならRealtime Database推奨）
- フロントエンドのみで完結させる（バックエンドサーバー不要）
- Firebase SDK は CDN から読み込み

---

## Firebase セットアップ

### プロジェクト作成（手動で行う前提）
- Firebase Console で新規プロジェクトを作成
- Realtime Database を有効化
- 認証は匿名認証（Anonymous Auth）を使用
- firebaseConfig はコード内に直接記述（プロトタイプ段階のため）

### セキュリティルール（プロトタイプ用、緩め）
```json
{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```
※本番化する際はルールを厳格化する

---

## マッチング方式: ルームコード共有

### フロー
1. プレイヤーA が「部屋を作る」を選択
2. ランダムな4桁の英数字ルームコードが生成・表示される
3. プレイヤーA はこのコードを相手に伝える（LINE、口頭など）
4. プレイヤーB が「部屋に入る」を選択し、コードを入力
5. 両者が揃ったらゲーム設定（サイコロ個数選択）→ ゲーム開始

### ルームコード仕様
- 4桁の英大文字+数字（例: A3K7）
- 紛らわしい文字（O/0、I/1/L）は除外
- 既存の部屋と重複しないようチェック
- 部屋は作成から5分経過で自動削除（相手が来なかった場合）
- ゲーム終了後も自動削除

---

## データ構造（Firebase Realtime Database）

```
rooms/
  {roomCode}/
    status: "waiting" | "setting" | "countdown" | "playing" | "finished"
    createdAt: timestamp
    settings/
      diceCount: 2 | 3
    players/
      host/
        id: "anonymous_uid"
        name: "プレイヤー1"
        ready: true | false
      guest/
        id: "anonymous_uid"
        name: "プレイヤー2"
        ready: true | false
    game/
      tokens/
        center: 3        # 中央の残り駒数
        host: 0           # ホストの獲得駒数
        guest: 0          # ゲストの獲得駒数
      dice/
        host/
          values: [3, 3, 3]    # 現在の出目
          rolling: false        # 回転中フラグ
          lastRollAt: timestamp
        guest/
          values: [2, 5, 1]
          rolling: false
          lastRollAt: timestamp
      log/                 # 直近のイベント（演出用）
        - { player: "host", action: "grab_success", at: timestamp }
        - { player: "guest", action: "grab_fail", at: timestamp }
      winner: null | "host" | "guest"
```

---

## ゲームの同期仕様

### 「振る」操作
1. プレイヤーが「振る」を押す
2. クライアント側でランダムに出目を生成
3. Firebase に自分の dice/values と lastRollAt を書き込む
4. 相手側はリアルタイムリスナーで出目の変更を検知し、相手のサイコロ表示を更新

### 「取る」操作（最重要：同時取得の制御）
1. プレイヤーが「取る」を押す
2. まず自分の出目がぞろ目かクライアント側で判定
3. **ぞろ目でない場合（誤申告）**:
   - トランザクションで自分の駒を-1、中央の駒を+1（駒が0なら何もしない）
   - 相手に誤申告イベントを通知（log に書き込み）
4. **ぞろ目の場合（正しい申告）**:
   - トランザクションで中央の駒を-1、自分の駒を+1
   - トランザクション内で中央の駒が0以下なら失敗（相手に先に取られた）
   - 成功→駒獲得演出、失敗→「間に合わなかった」演出
5. 駒が2になったら winner を書き込み → 両クライアントで勝敗画面表示

**トランザクション処理がこのゲームの通信対戦の肝。** 同時に「取る」が押された場合、Firebaseのトランザクションが先着順で処理するため、自然に早い者勝ちが実現できる。

### 相手の状態表示
- 相手のサイコロの出目はリアルタイムで表示する（CPU対戦と同じUIを流用）
- 相手が振った時の回転アニメーションも表示

---

## UI変更

### タイトル画面の変更
```
[ サイコロぞろ目バトル ]

  [🤖 CPU対戦]     [🌐 オンライン対戦]
```

### CPU対戦を選んだ場合
- 現在と同じフロー（サイコロ個数 → 難易度 → ゲーム開始）

### オンライン対戦を選んだ場合
```
  [🏠 部屋を作る]    [🚪 部屋に入る]
```

#### 「部屋を作る」画面
```
  ルームコード: A3K7
  （このコードを相手に伝えてください）
  
  相手を待っています...
  
  [キャンセル]
```

#### 「部屋に入る」画面
```
  ルームコードを入力:
  [____]
  
  [入室]  [戻る]
```

#### 両者入室後（設定画面）
- ホスト側がサイコロ個数（2個/3個）を選択
- ゲスト側は選択を待つ表示
- ホストが選択完了 → カウントダウン → ゲーム開始

### ゲーム中UI
- CPU対戦時とほぼ同じレイアウト
- 画面上部: 相手のサイコロ（リアルタイム同期表示）
- 画面中央: 駒の状態
- 画面下部: 自分のサイコロ + 操作ボタン
- 相手の名前表示（「プレイヤー1」「プレイヤー2」程度でOK）

### ゲーム終了後
```
  WIN! / LOSE...
  
  [もう一度（同じ相手）]  [タイトルへ]
```
「もう一度」は両者が押した場合に再戦開始（片方だけなら待機表示）

---

## 切断・エラー処理

### 相手が切断した場合
- Firebase の `.info/connected` と `onDisconnect` を使用
- 相手の接続が切れたら「相手が切断しました」と表示し、タイトルに戻るボタンを表示
- 部屋データはクリーンアップ

### 通信エラー
- Firebase への書き込み失敗時はリトライ（最大3回）
- それでも失敗したら「通信エラー」表示

### ゲーム中のブラウザバック・タブ閉じ
- `onDisconnect` でプレイヤーの離脱を検知
- 残ったプレイヤー側に切断通知

---

## 実装の優先度

1. **最優先**: Firebase接続・匿名認証・部屋の作成/入室
2. **高**: サイコロの振る操作の同期・表示
3. **高**: 「取る」操作のトランザクション処理（同時取得の制御）
4. **中**: 勝敗判定・結果画面
5. **中**: 切断検知・エラー処理
6. **低**: 再戦機能

---

## 注意事項

- 既存のCPU対戦モードは一切変更しないこと
- オンライン対戦のゲームロジック（ぞろ目判定、ペナルティ等）はCPU対戦と同一
- ぞろ目のヒント演出は入れないこと（CPU対戦と同じ方針）
- Firebase の config 値はプレースホルダーで記述し、コメントで「ここに自分のFirebase設定を入れる」と明記
- まずは動く状態を優先し、細かいUIの調整は後で行う
