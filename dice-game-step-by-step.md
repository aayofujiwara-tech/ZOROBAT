# サイコロぞろ目バトル - 通信対戦 段階実装ガイド

## 前提
- 実装仕様は `dice-game-online-prompt.md` に記載済み（リポジトリ内にあります）
- 必ず先に `dice-game-online-prompt.md` を読んでから作業してください
- 各ステップを1つずつ実装し、動作確認できる状態でコミットしてください
- 既存のCPU対戦モードは絶対に壊さないこと

---

## ステップ1: Firebase SDK導入 + 匿名認証

### やること
- Firebase SDK を CDN から読み込み（firebase-app, firebase-auth, firebase-database）
- `dice-game-online-prompt.md` に記載の firebaseConfig を設定
- ページ読み込み時に匿名認証を実行
- 認証成功をコンソールログで確認できるようにする

### やらないこと
- UI変更はまだしない
- ルーム機能はまだ作らない

### 動作確認
- ブラウザのコンソールに匿名認証のUIDが表示されればOK
- 既存のCPU対戦が正常に動作すること

### 完了したらコミットして停止。ステップ2には絶対に進まないこと。

---

## ステップ2: タイトル画面の分岐UI

### やること
- タイトル画面に「CPU対戦」「オンライン対戦」の選択を追加
- 「CPU対戦」→ 既存のフロー（サイコロ個数 → 難易度 → ゲーム開始）
- 「オンライン対戦」→ 「部屋を作る」「部屋に入る」の選択画面
- 「部屋を作る」→ ルームコード表示 + 待機画面
- 「部屋に入る」→ コード入力画面
- UIデザインは既存のダークテーマ/カード型UIに合わせる

### やらないこと
- 実際のFirebaseへの部屋作成はまだしない（UIの遷移だけ作る）

### 動作確認
- 各画面の遷移が正しく動くこと
- 「戻る」「キャンセル」で前の画面に戻れること
- CPU対戦が引き続き正常に動作すること

### 完了したらコミットして停止。ステップ3には絶対に進まないこと。

---

## ステップ3: ルーム作成・入室（Firebase連携）

### やること
- 「部屋を作る」で Firebase Realtime Database にルームを作成
- 4桁ルームコード生成（紛らわしい文字除外: O/0/I/1/L）
- ルームデータ構造は `dice-game-online-prompt.md` に従う
- 「部屋に入る」でコード入力 → 該当ルームにゲスト参加
- 両者が揃ったら status を "setting" に変更
- ホスト側にサイコロ個数の選択UIを表示
- ゲスト側は「ホストが設定中...」の待機表示
- 部屋の自動削除（5分タイムアウト）
- 存在しないコード入力時のエラー表示

### 動作確認
- ブラウザ2つ（またはタブ2つ）で部屋作成→入室ができること
- Firebase Console のデータベースにルームデータが作成されていること
- 両者入室後にサイコロ個数選択画面に遷移すること

### 完了したらコミットして停止。ステップ4には絶対に進まないこと。

---

## ステップ4: ゲーム開始 + サイコロ同期

### やること
- ホストがサイコロ個数を選択 → カウントダウン → ゲーム開始
- 「振る」操作時に自分の出目を Firebase に書き込み
- 相手の出目をリアルタイムリスナーで受信し画面に反映
- 相手のサイコロの回転アニメーションも表示
- ゲーム画面のレイアウトはCPU対戦と同じものを流用

### やらないこと
- 「取る」操作はまだ実装しない（振るの同期だけ）

### 動作確認
- 2つのブラウザでゲームを開始し、互いのサイコロが同期表示されること
- 振るたびに相手側にリアルタイムで反映されること
- 遅延が体感的に気にならないレベルであること

### 完了したらコミットして停止。ステップ5には絶対に進まないこと。

---

## ステップ5: 「取る」操作 + 駒のトランザクション処理

### やること
- 「取る」ボタンの実装
- ぞろ目判定（クライアント側）
- 正しい申告 → トランザクションで中央の駒-1、自分の駒+1
- 誤申告 → トランザクションで自分の駒-1、中央の駒+1（駒0なら何もしない）
- 同時取得の制御（トランザクションで中央の駒が0以下なら失敗）
- 駒の状態変更をリアルタイムリスナーで両者に反映
- 取得成功/失敗/誤申告の演出

### 動作確認
- 2つのブラウザで「取る」が正しく動作すること
- 誤申告のペナルティが正しく適用されること
- 残り1個の駒を同時に取ろうとした場合、片方だけが取れること
- 駒の表示が両者でリアルタイムに同期すること

### 完了したらコミットして停止。ステップ6には絶対に進まないこと。

---

## ステップ6: 勝敗判定 + 結果画面 + 切断処理

### やること
- 駒が2個になったら winner を Firebase に書き込み
- 両クライアントで勝敗画面を表示（WIN! / LOSE...）
- 「もう一度」ボタン（両者が押したら再戦）
- 「タイトルへ」ボタン
- `onDisconnect` による切断検知
- 相手切断時の「相手が切断しました」表示
- ゲーム終了後のルームデータクリーンアップ

### 動作確認
- 勝敗が正しく判定・表示されること
- 片方がタブを閉じた場合、もう片方に切断通知が出ること
- 再戦機能が動作すること
- ゲーム終了後にFirebase上のルームデータが削除されること

### 完了したらコミットして停止。全ステップ完了を報告すること。

---: 1ステップずつ必ず停止すること

**絶対に複数ステップを連続で実装しないでください。**

- 1つのステップを実装し終えたら、必ずコミットして **作業を完全に停止** してください
- 次のステップには **絶対に自動で進まないで** ください
- 停止時に「ステップ○が完了しました。動作確認後、次のステップの指示をお願いします。」と報告してください
- ユーザーが明示的に「次のステップに進んで」と指示するまで、絶対に次の作業を開始しないでください
- この停止ルールは最優先事項です。効率化のために複数ステップをまとめることは禁止です

## Claude Code への指示方法

**ステップ1を開始する時：**
```
リポジトリ内の dice-game-online-prompt.md と dice-game-step-by-step.md を両方読んでください。
通信対戦機能の「ステップ1: Firebase SDK導入 + 匿名認証」だけを実装してください。
ステップ1が完了したらコミットして停止してください。ステップ2には進まないでください。
```

**ステップ1完了の報告を受けたら、動作確認してからステップ2を指示：**
```
ステップ1の動作確認OKです。「ステップ2: タイトル画面の分岐UI」だけを実装してください。
完了したらコミットして停止。ステップ3には進まないでください。
```

**以降も同じパターンで、ステップ6まで1つずつ進めてください。**
